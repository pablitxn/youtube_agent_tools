apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# This overlay only contains image references for ArgoCD multi-source setup
# Base configuration and sensitive configs are in the private infrastructure repo

images:
  - name: ghcr.io/pablitxn/youtube-agent-tools
    newTag: prod
    # CI/CD will update this with digest, e.g.:
    # digest: sha256:abc123...
